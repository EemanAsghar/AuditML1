model:
  dataset: cifar10
  name: resnet18

data:
  dataset: cifar10

train:
  epochs: 30
  batch_size: 128

attack:
  attack_type: mia_threshold

dp:
  enabled: true
  epsilon: 1.0
  delta: 1.0e-5
  max_grad_norm: 1.0
